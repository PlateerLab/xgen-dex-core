name: CI

# 모든 PR 과 main push. 세 앱과 세 패키지를 **한 번에** 검사한다 — 코어를 고치면
# 그 자리에서 세 표면이 같이 돌아야, 한쪽만 깨진 채로 머지되는 일이 없다.

on:
  pull_request:
  push:
    branches: [main]

jobs:
  # 모바일(React Native)은 별도 npm 프로젝트 — 다른 표면과 완전히 독립이라 잡도
  # 분리한다 (여기 실패해도 verify 산출과 무관, verify 실패와도 무관).
  mobile:
    name: 모바일 (타입 · 테스트)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: apps/mobile/package-lock.json
      - run: npm --prefix apps/mobile ci --no-audit --no-fund
      - run: npm --prefix apps/mobile test
      - run: npm --prefix apps/mobile run typecheck

  # ── 캐시 워밍 (main 푸시에서만) ──
  #
  # 태그(릴리스) 런은 **main 에 저장된 캐시만** 복원할 수 있다 — 태그 런이
  # 스스로 저장한 캐시는 다음 태그가 못 쓴다. 그래서 릴리스가 빨라지려면
  # main 푸시 시점에 gradle 의존성과 iOS 컴파일 캐시(ccache)를 미리 만들어
  # 둬야 한다. PR 검증과 무관하게 병렬로 돌고, 실패해도 아무것도 막지 않는다.
  warm-android:
    name: 캐시 워밍 — 안드로이드
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: apps/mobile/package-lock.json
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21
      - uses: gradle/actions/setup-gradle@v4
        with:
          cache-read-only: false
      - run: npm --prefix apps/mobile ci --no-audit --no-fund
      - name: APK 빌드 (debug 폴백 키 — 캐시 생성용)
        working-directory: apps/mobile/android
        run: ./gradlew assembleRelease

  warm-ios:
    name: 캐시 워밍 — iOS
    if: github.event_name == 'push'
    runs-on: macos-15
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: apps/mobile/package-lock.json
      - name: Xcode 16 선택
        run: |
          XC=$(ls -d /Applications/Xcode_16*.app 2>/dev/null | sort -V | tail -1)
          [ -n "$XC" ] && sudo xcode-select -s "$XC"
          xcodebuild -version
      - run: npm --prefix apps/mobile ci --no-audit --no-fund
      - name: ccache 설치
        run: brew install ccache || true
      # ⚠ CCACHE_DIR env 는 쓰지 않는다 — xcodebuild 의 컴파일은 별도 빌드 서비스
      # 프로세스에서 돌아 CLI env 를 물려받지 않는다(실측: 커스텀 디렉터리 0.0GiB).
      # macOS 기본 캐시 디렉터리를 그대로 쓰고 그 경로를 저장한다.
      - uses: actions/cache@v4
        with:
          path: ~/Library/Caches/ccache
          key: ios-ccache-${{ hashFiles('apps/mobile/package-lock.json') }}
          restore-keys: ios-ccache-
      - uses: actions/cache@v4
        with:
          path: |
            apps/mobile/ios/Pods
            ~/Library/Caches/CocoaPods
          key: ios-pods-${{ hashFiles('apps/mobile/package-lock.json', 'apps/mobile/ios/Podfile', 'apps/mobile/ios/Podfile.properties.json') }}
          restore-keys: ios-pods-
      - name: Pod install
        working-directory: apps/mobile/ios
        run: pod install
      - name: Xcode 빌드 (캐시 생성용)
        working-directory: apps/mobile/ios
        run: |
          xcodebuild -workspace XGENDex.xcworkspace -scheme XGENDex \
            -configuration Release -sdk iphoneos -derivedDataPath build \
            CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO build
      - name: ccache 통계
        run: ccache -s || true

  verify:
    name: 계약 · 타입 · 테스트
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      # 데스크톱은 workspace 밖이라(electron) 따로 설치한다.
      - run: npm ci --no-audit --no-fund
      - run: npm --prefix apps/desktop ci --no-audit --no-fund

      # 산출물이 필요한 검사(확장 번들에 엔진이 없는지)가 있으므로 빌드가 먼저다.
      - name: 빌드
        run: |
          npm --prefix apps/cli run build
          npm --prefix apps/vscode run build
          npm --prefix apps/desktop run build

      - name: 계약 검사
        run: npm run contracts

      - name: 타입체크
        run: |
          npx tsc --noEmit -p packages/protocol/tsconfig.json
          npx tsc --noEmit -p packages/engine/tsconfig.json
          npx tsc --noEmit -p packages/rpc/tsconfig.json
          npx tsc --noEmit -p apps/cli/tsconfig.json
          npx tsc --noEmit -p apps/vscode/tsconfig.json

      - name: 테스트
        run: |
          npm --prefix packages/protocol test
          npm --prefix packages/engine test
          npm --prefix apps/cli test
          npm --prefix apps/vscode test
          npm --prefix apps/desktop test
