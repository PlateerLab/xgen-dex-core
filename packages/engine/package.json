{
  "name": "@dex/engine",
  "version": "1.29.0",
  "private": true,
  "type": "module",
  "description": "이 기기에서 실제로 무언가를 하는 것 — 로컬 도구 · MCP · 서버 브릿지. Electron 은 모른다.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "scripts": {
    "check": "tsc --noEmit -p tsconfig.json",
    "test": "node --import tsx --test test/*.test.ts"
  },
  "dependencies": {
    "ws": "^8.21.0",
    "@modelcontextprotocol/sdk": "^1.29.0"
  }
}
