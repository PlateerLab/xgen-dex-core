/**
 * @dex/rpc — 엔진을 다른 프로세스에 빌려주는 법.
 *
 * VSCode 확장은 편집기 프로세스 안에서 돌지만 셸을 띄우고 키체인을 읽어야 한다.
 * 그래서 엔진을 자식 프로세스로 띄우고 JSON-RPC 로 말한다 — LSP 와 같은 모양이라
 * 편집기 개발자에게 익숙하고, `initialize` 에서 버전과 capabilities 를 맞춰 보므로
 * 한쪽만 오래된 상태를 첫 왕복에서 잡는다.
 *
 * **진입점이 셋인 이유.** 이 파일은 오가는 것의 *모양*만 준다. 서버와 클라이언트는
 * 각각 `@dex/rpc/server`, `@dex/rpc/client` 로 따로 가져간다.
 *
 * 한 파일에서 다 내보냈더니 VSCode 확장이 클라이언트를 가져오는 것만으로 서버 →
 * 엔진 → keytar(네이티브 .node) 까지 끌고 와 번들이 깨졌다. 확장은 엔진을 **자식
 * 프로세스로 띄우는** 쪽이지 안에 품는 쪽이 아니다 — 그 경계가 여기서 물리적으로
 * 지켜진다.
 */
export * from './wire';
